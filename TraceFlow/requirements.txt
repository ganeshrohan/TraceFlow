# Trial account: no External Access Integration is possible, so nothing can be fetched from PyPI.
# All runtime dependencies come from the pre-installed streamlit[snowflake] (which includes pandas).
# Do not add packages here or in pyproject.toml - it will trigger a PyPI fetch that fails.
# sqlglot is vendored in the sqlglot/ folder (pure Python). Graph rendering is dependency-free
# (custom HTML/SVG card map with a Graphviz fallback) - no external JS libraries.
